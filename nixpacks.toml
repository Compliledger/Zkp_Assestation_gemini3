[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "postgresql", "gcc", "libpqxx"]

[phases.install]
cmds = [
    "cd gemini3-zkp-attestation-agent && python3 -m pip install --upgrade pip && python3 -m pip install --no-cache-dir -r requirements.txt"
]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "cd gemini3-zkp-attestation-agent && python3 -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
